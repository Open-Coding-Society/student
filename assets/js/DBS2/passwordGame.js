// passwordGame has been retired. This module is now a no-op stub kept for compatibility.
export default function beginQuiz(){
    console.log('beginQuiz() was called, but the password quiz has been removed.');
}